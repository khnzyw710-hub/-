# תיקון "Context limit reached" ב-Claude Code

מסמך זה מסביר למה הופיעה ההודעה `Context limit reached · /compact or /clear to continue`
ומה בדיוק תוקן. הקובץ הזה **לא** נטען אוטומטית לזיכרון של Claude — הוא רק לתיעוד.

---

## ⚠️ עדכון: אם אחרי התיקון הטרמינלים "מתנתקים" — ביטול מלא

אם אחרי הרצת התיקון במחשב שלך Claude Code נסגר/מתנתק מיד אחרי שנותנים לו פקודה
ומשאיר טרמינל רגיל — יש דרך חזרה מלאה ובטוחה. הסקריפט
[`restore-terminals.sh`](.claude/restore-terminals.sh):

- מסיר את בלוק ה-export שהתיקון הוסיף לפרופיל של הטרמינל (`.zshrc` / `.bash_profile` וכו')
- מסיר **בדיוק** את המפתחות שהתיקון הוסיף ל-`~/.claude/settings.json` (ולא נוגע בשום דבר אחר)
- מזהה קבצי מצב פגומים (`~/.claude.json` לא-תקין — גורם קלאסי לקריסה מיד אחרי פקודה) ומזיז אותם הצידה
- שומר גיבוי `.restore-bak` לכל קובץ לפני כל שינוי, ובטוח להרצה חוזרת

**הרצה במחשב שלך** (בטרמינל רגיל, מתוך תיקיית המאגר):

```bash
bash .claude/restore-terminals.sh
```

ואם אין לך את המאגר על המחשב — פקודת הדבקה אחת שמורידה ומריצה מהענף:

```bash
curl -fsSL https://raw.githubusercontent.com/khnzyw710-hub/-/claude/claude-code-terminals-b7wh24/.claude/restore-terminals.sh | bash
```

אחרי ההרצה: **לסגור את כל חלונות הטרמינל הפתוחים**, לפתוח חלון חדש, ולהריץ `claude`.

מי שרוצה לשמור את כוונון הדחיסה (שאינו חשוד בקריסה) ולהסיר רק את
`ENABLE_TOOL_SEARCH` — החשוד המרכזי — מריץ עם `--keep-compact`.
לבדיקה בלבד בלי לשנות כלום: `--check`.

**מה כנראה קרה:** התיקון כפה `ENABLE_TOOL_SEARCH=true` גלובלית. במסלולים שבהם השרת/שער
אינו תומך בבלוקים של `tool_reference` (האזהרה כתובה בסקריפט התיקון עצמו), כל פנייה נכשלת —
ומבחוץ זה נראה כמו "נתתי פקודה וזה התנתק". בנוסף, קריסות חוזרות יכולות להשחית את
`~/.claude.json`, מה שגורם ל-Claude Code להיסגר גם אחרי הביטול — ולכן הסקריפט בודק ומתקן גם את זה.
`ENABLE_TOOL_SEARCH` הוסר גם מ-`.claude/settings.json` של המאגר, כדי ששיבוט מקומי לא יחזיר את הבעיה.

---

## מה נמדד בפועל

לא ניחוש — מדידה מתוך יומן הסשן (`~/.claude/projects/*/*.jsonl`):

| רכיב | טוקנים בתחילת סשן |
|---|---|
| סך הכל בפתיחת סשן, לפני מילה ראשונה | **41,707** |
| כל ה-skills ביחד (תיאורים בלבד) | ~2,100 |
| hooks (`SessionStart`, `UserPromptSubmit`) | 0 |
| קבצי `CLAUDE.md` בפרויקט | 0 (לא קיימים) |

**מסקנה: ה-skills לא היו הבעיה** — הם 5% מהבסיס. גם ה-hooks לא. מחיקת skills לא היתה עוזרת.

## הסיבה האמיתית

הפעם לא ניחוש ולא הסתמכות על מקורות חיצוניים — הקוד של Claude Code עצמו
(`/opt/claude-code/bin/claude`) פורק ונקרא. זה מה שקורה בפועל:

**ההודעה נכתבת מקומית, לפני שנשלחת בקשה כלשהי לשרת.**
לפני כל פנייה Claude Code סופר את הטוקנים של כל השיחה ומשווה לסף. אם עברנו את הסף,
הוא מדפיס את ההודעה ומפסיק — בלי לפנות ל-API בכלל. זה מסביר למה ההודעה מופיעה מיד,
בלי המתנה: זו לא שגיאה מהשרת.

**הסף המדויק:** `חלון_אמיתי − 20,000 − 3,000`. עבור מודל של 200K טוקנים זה
**177,000 טוקנים**. מעל המספר הזה — חסום.

**שתי נקודות שחשוב להבין:**

1. `autoCompactWindow` **לא** מזיז את קו החסימה (177,000). הוא מזיז את הקו שבו
   הדחיסה האוטומטית *מתחילה*. זה בדיוק העניין — צריך לדחוס הרבה לפני 177,000.

2. **הפער בברירת המחדל הוא הבעיה.** בהגדרות ברירת מחדל הדחיסה נכנסת ב-167,000
   והחסימה ב-177,000 — **מרווח של 10,000 טוקנים בלבד**. במקביל, ברירת המחדל של
   `MAX_MCP_OUTPUT_TOKENS` היא **25,000 טוקנים לקריאה בודדת**. כלומר תוצאה אחת גדולה
   מכלי MCP קופצת מעל כל המרווח בבת אחת: הדחיסה לא מספיקה להיכנס, ונוחתים ישר בחסימה.
   זה שורש הבעיה.

**ולמה זה קרה "בטרמינל חדש", ובלי הרבה עבודה?**

יש שתי דרכים להגיע ל-177,000, ורק אחת מהן קשורה לאורך השיחה:

1. **שיחה שממשיכה** — דרך `--continue` / `--resume`, שחזור אוטומטי של האפליקציה או
   ה-IDE, או פתיחה מחדש של אותו סשן ב-claude.ai/code. שיחה שכבר חצתה את הקו נשארת
   מעליו, ולכן כל פתיחה מחדש מפעילה את ההודעה שוב מיד.

2. **הבסיס עצמו גדול מדי — וזה המקרה שמסביר "לא עבדתי הרבה".**
   סכימות הכלים של ה-MCP (ה-connectors) נטענות באחת משתי צורות:

   - **ToolSearch פעיל** — לקלוד נשלחת רק *רשימת השמות* של הכלים, והסכימה המלאה
     נמשכת רק כשכלי מסוים באמת נחוץ. זה מה שקורה בסביבה הזו, ולכן הבסיס כאן הוא
     41,710 טוקנים בלבד.
   - **ToolSearch כבוי** — **כל** הסכימות של **כל** הכלים נדחפות לפרומפט המערכת בפתיחת
     הסשן. עם ~250 כלים (Higgsfield 85, Shopify 26, Canva, Notion, Base44, Figma,
     Gmail, Calendar, Drive, github) זה יכול לבדו לעבור את 177,000 — כלומר טרמינל
     נקי לגמרי, לפני מילה אחת, כבר חסום.

   בקוד (`aJr()` / `U4()`) ToolSearch נכבה בשקט בכמה מצבים: כש-`ENABLE_TOOL_SEARCH`
   לא מוגדר וה-`ANTHROPIC_BASE_URL` אינו מארח ראשוני של Anthropic (פרוקסי, gateway,
   Bedrock/Vertex), כשהוא מוגדר לערך כבוי, או כש-`CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS`
   דלוק. זה גם מסביר למה ניתוק שני connectors עזר רק מעט — הבעיה היא **איך** הכלים
   נטענים, לא כמה מהם יש.

**חשוב לדעת — מה שאני רואה מהצד שלי:**
היומן של הסשן הזה נמדד במלואו: אפס שגיאות API, המקסימום שהשיחה הגיעה אליו הוא
109,484 טוקנים מול קו חסימה של 177,000, ושש דחיסות אוטומטיות שכולן הצליחו.
כלומר **בסביבה הזו החסימה מעולם לא נורתה**. ההודעה שמופיעה בטרמינל שלך מגיעה
מהתקנת Claude Code על המחשב שלך — סביבה נפרדת שהקונטיינר הזה לא רואה ולא יכול
לשנות. לכן קובץ ההגדרות חייב להיות מוחל שם, על המחשב עצמו (ראה הפקודה למטה).

## מה תוקן

`.claude/settings.json`:

```json
{
  "autoCompactWindow": 120000,
  "autoCompactEnabled": true,
  "env": {
    "CLAUDE_CODE_AUTO_COMPACT_WINDOW": "120000",
    "MAX_MCP_OUTPUT_TOKENS": "10000",
    "ENABLE_TOOL_SEARCH": "true"
  }
}
```

| הגדרה | מה היא עושה |
|---|---|
| `ENABLE_TOOL_SEARCH: "true"` | **המפתח החשוב ביותר לטרמינל נקי.** מכריח טעינה עצלה של סכימות הכלים: נשלחים רק שמות הכלים, והסכימה המלאה נמשכת רק בשימוש בפועל. בלעדיו, בהתקנה שעוברת דרך פרוקסי או gateway, כל הסכימות נדחפות בפתיחה — וזה לבדו יכול לחסום סשן ריק. |
| `autoCompactWindow: 120000` | מקדים את הדחיסה האוטומטית ל-~87,000 טוקנים במקום 167,000. במקום מרווח של 10,000 עד החסימה — מרווח של ~90,000. |
| `autoCompactEnabled: true` | מוודא שהדחיסה האוטומטית בכלל דלוקה. אם היא כבויה, כל שאר ההגדרות חסרות משמעות — השיחה פשוט תגדל עד לחסימה. |
| `CLAUDE_CODE_AUTO_COMPACT_WINDOW` | אותו ערך כמשתנה סביבה. הקוד קורא אותו **לפני** קובץ ההגדרות, והוא גם עובר לתת-סוכנים. חגורה ושתי כתפיים. |
| `MAX_MCP_OUTPUT_TOKENS: 10000` | חוסם תוצאה בודדת מכלי MCP מלתפוס יותר מ-10K טוקנים במקום 25K. זה מה שמונע את הקפיצה מעל המרווח. |

התוצאה: הבסיס בפתיחה נשאר קטן (~42,000 במקום פוטנציאל של 177,000+), הדחיסה נכנסת
ב-~87,000, קו החסימה ב-177,000, ואף תוצאת כלי בודדת לא יכולה לגשר על ~90,000 טוקנים.

**אימות שההגדרות עובדות:** בסשן הזה חמש הדחיסות האחרונות נורו ב-79,862 / 80,509 /
80,894 / 82,631 / 83,459 טוקנים — בדיוק מול הסף הצפוי של 80,000 שמחושב מ-`autoCompactWindow: 120000`.
כלומר ההגדרה לא רק נכתבה, היא נמדדה בפעולה.

## איפה זה כבר פעיל — בלי לעשות כלום

- **בסשנים מ-claude.ai/code על הריפו הזה:** פעיל אוטומטית. הקובץ `.claude/settings.json`
  נמצא בריפו, נטען לבד בכל פתיחת סשן. אין שום פעולה ידנית.
- **בסביבת הריצה הנוכחית:** הוחל גם על `~/.claude/settings.json` (הגדרות המשתמש),
  שחלות על כל הפרויקטים בסביבה הזו.

## הכיסוי המלא — `fix-context-limit.sh`

ההגדרות למעלה מטפלות בגורם המרכזי. אבל יש עוד מצבים שיכולים לגרום לאותה הודעה,
וחלקם **גוברים** על קובץ ההגדרות. כדי לא לקחת סיכון, הסקריפט
`.claude/fix-context-limit.sh` בודק את **כולם** ומדווח על כל אחד בנפרד:

```bash
bash .claude/fix-context-limit.sh            # בודק, מתקן, ומדפיס דוח מלא
bash .claude/fix-context-limit.sh --check    # בדיקה בלבד — לא נוגע בשום קובץ
bash .claude/fix-context-limit.sh --no-profile # בלי לגעת ב-.zshrc/.bashrc
```

**חוזה הבטיחות של הסקריפט — הוא לא מוחק כלום.**

- הוא רק **מוסיף** מפתחות לקובץ ההגדרות. כל מפתח קיים — `model`, `permissions`,
  `theme`, משתני סביבה משלך — נשאר בדיוק כמו שהוא.
- כל קובץ שהוא כותב מגובה קודם ל-`<קובץ>.bak`.
- כל דבר שדורש **הסרה** רק מזוהה ומדווח, ואתה מחליט. הסקריפט לא נוגע בו.
- אם כבר בחרת ערך **מחמיר יותר** משלנו (למשל `MAX_MCP_OUTPUT_TOKENS=4000`),
  הסקריפט משאיר את שלך ומדפיס `kept your stricter value`. הוא לעולם לא מרפה הגדרה.
- בטוח להרצה חוזרת. בהרצה שנייה הוא מדפיס `was already correct - nothing changed`.

### מה הוא בודק — 16 הגורמים

| # | גורם | הסקריפט |
|---|---|---|
| 1 | שיחה שממשיכה וכבר מעל קו החסימה | מדווח + מנחה ל-`/clear` |
| 2 | סכימות MCP נטענות בפתיחה (ToolSearch כבוי) | **מתקן** — `ENABLE_TOOL_SEARCH=true` |
| 3 | `CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS` דלוק | מדווח כחוסם (מבטל את ToolSearch) |
| 4 | מצב hipaa | מדווח כחוסם |
| 5 | `ANTHROPIC_BASE_URL` מצביע על פרוקסי | מדווח — חייב להעביר `tool_reference` |
| 6 | דחיסה אוטומטית כבויה (`DISABLE_COMPACT` / `DISABLE_AUTO_COMPACT` / `autoCompactEnabled:false`) | **מתקן** את המפתח בהגדרות, מדווח על משתני הסביבה |
| 7 | חלון הדחיסה מאוחר מדי | **מתקן** — `autoCompactWindow=120000` |
| 8 | תוצאות MCP גדולות | **מתקן** — `MAX_MCP_OUTPUT_TOKENS=10000` |
| 9 | `CLAUDE_CODE_MAX_CONTEXT_TOKENS` נמוך | מדווח — מכווץ את החלון וחוסם טרמינל ריק |
| 10 | `MAX_THINKING_TOKENS` גדול | מדווח |
| 11 | `managed-settings.json` ארגוני שגובר על הכל | סורק את כל הנתיבים לפי מערכת ההפעלה |
| 12 | export ב-shell שגובר על `settings.env` | סורק את קבצי הפרופיל, **בלי לערוך אותם** |
| 13 | `CLAUDE.md` גדול | מודד ומדווח |
| 14 | הרבה שרתי MCP | סופר מ-`~/.claude.json` ומ-`.mcp.json` |
| 15 | `.jsonl` של סשן ישן וענק | מזהה ומדווח |
| 16 | `CLAUDE_CODE_BLOCKING_LIMIT_OVERRIDE` | **בכוונה לא מופעל** — ראה למטה |

### למה `CLAUDE_CODE_BLOCKING_LIMIT_OVERRIDE` לא מופעל

זה המשתנה שמזיז את קו החסימה עצמו (177,000). מפתה — אבל הוא מזיז רק את **הבדיקה
המקומית**, לא את המגבלה האמיתית של ה-API. התוצאה: במקום הודעה מקומית נקייה לפני
שנשלח משהו, מקבלים שגיאת 400 מהשרת באמצע העבודה. כלומר הוא **מגדיל** את הסיכון
במקום להקטין אותו. לכן הוא מזוהה ומוסבר, אבל לא נדלק.

### משתני הסביבה בקובץ הפרופיל

משתני סביבה של התהליך גוברים על `settings.env`. לכן הסקריפט מוסיף גם בלוק מסומן
לקובץ הפרופיל (`.zshrc` / `.bash_profile` / `.bashrc` — לפי `$SHELL`):

```bash
# --- claude-code context fix (added by fix-context-limit.sh) ---
export ENABLE_TOOL_SEARCH=true
export CLAUDE_CODE_AUTO_COMPACT_WINDOW=120000
export MAX_MCP_OUTPUT_TOKENS=10000
# --- end claude-code context fix ---
```

הבלוק מסומן, מגובה ל-`.bak`, ונוסף פעם אחת בלבד. הערכים המיוצאים נקראים בחזרה
מקובץ ההגדרות שנכתב — כך שאם נשמר שם ערך מחמיר יותר, גם ה-export ישקף אותו ולא
ירפה אותו. אם אתה מעדיף לא לגעת בפרופיל: `--no-profile`.

## על המחשב שלך — פקודה אחת, הדבק והרץ

זה החלק שאני **לא** יכול לעשות מכאן: הטרמינל שמראה את ההודעה רץ על המחשב שלך,
והקונטיינר הזה לא נוגע בו.

**יש לך שכפול של הריפו?** אז זו הדרך המלאה, בטרמינל רגיל (לא בתוך Claude Code):

```bash
bash .claude/fix-context-limit.sh
```

**אין לך שכפול?** הפקודה הבאה עושה את החלק המרכזי בלבד — לא צריך לשכפל שום דבר,
לא צריך שום שלב נוסף:

```bash
python3 - <<'PY'
import json, os, shutil, pathlib
d = pathlib.Path(os.environ.get("CLAUDE_CONFIG_DIR") or (pathlib.Path.home() / ".claude"))
d.mkdir(parents=True, exist_ok=True)
f = d / "settings.json"
s = {}
if f.exists():
    shutil.copy(f, str(f) + ".bak")
    try:
        s = json.loads(f.read_text() or "{}")
    except Exception:
        s = {}
if not isinstance(s, dict):
    s = {}
s["autoCompactWindow"] = 120000
e = s.get("env")
if not isinstance(e, dict):
    e = {}
e.update({
    "CLAUDE_CODE_AUTO_COMPACT_WINDOW": "120000",
    "MAX_MCP_OUTPUT_TOKENS": "10000",
    "ENABLE_TOOL_SEARCH": "true",
})
s["env"] = e
f.write_text(json.dumps(s, indent=2, ensure_ascii=False) + "\n")
print("OK ->", f)
print(f.read_text())
PY
```

היא מגבה קובץ קיים ל-`settings.json.bak`, ממזגת רק את ארבעת הערכים האלה, ולא מוחקת
שום דבר אחר — `model`, `permissions`, כל שאר ההגדרות נשארות בדיוק כמו שהן.
אחרי ההרצה: סוגרים את הטרמינל של Claude Code ופותחים מחדש.

## אם עובדים גם מקומית, בתיקיות אחרות

הגדרות של ריפו חלות רק בתוך הריפו. בתוך שכפול של הריפו הזה יש שני סקריפטים:

```bash
bash .claude/fix-context-limit.sh   # מומלץ — בודק את כל 16 הגורמים ומתקן
bash .claude/apply-globally.sh      # מינימלי — רק כותב את ההגדרות
```

שניהם בטוחים להרצה חוזרת: מגבים קובץ קיים ל-`.bak` וממזגים רק את המפתחות הרלוונטיים,
בלי למחוק שום דבר אחר.

**להחלה קבועה ב-claude.ai/code:** הקונטיינר המרוחק נמחק בסוף כל סשן, ולכן
`~/.claude/settings.json` לא שורד שם — אבל `.claude/settings.json` שבריפו כן, ולכן
בריפו הזה זה מכוסה. כדי שיחול על **כל** הריפואים בסביבה, מוסיפים את שלושת משתני
הסביבה להגדרות הסביבה עצמה:

```
ENABLE_TOOL_SEARCH=true
CLAUDE_CODE_AUTO_COMPACT_WINDOW=120000
MAX_MCP_OUTPUT_TOKENS=10000
```

## על skills — נבדק ונדחה

השאלה היתה אם אפשר להשאיר skills מותקנים בלי שייטענו בפתיחה, ושהם ייטענו רק לפי צורך.
נבדקו שתי דרכים:

**`skillOverrides` ב-settings.json — לא מתאים.**
בניגוד למה שמופיע בכמה מקורות, ההגדרה הזו **משביתה** skill לחלוטין. הבינארי מחזיר:
`Skill "..." is disabled via skillOverrides. Re-enable it in /skills or remove the
override from your settings to run it.` כלומר ה-skill לא יהיה זמין גם בהפעלה ידנית —
ההפך מהמטרה.

**`disable-model-invocation` — שדה של ה-skill, לא של ההגדרות.**
זהו שדה ב-frontmatter של `SKILL.md` עצמו, לא ערך בתוך `skillOverrides`. הוא אכן הופך
skill ל"ניתן להפעלה ידנית בלבד", אבל דורש עריכה של כל קובץ skill בנפרד, וה-skills
מסונכרנים מהחשבון ב-claude.ai ולכן עריכה מקומית עלולה להידרס.

**בכל מקרה החיסכון לא מצדיק את זה:** כל תיאורי ה-skills ביחד הם ~2,100 טוקנים — 5%
מהבסיס. זה לא מה שגרם לבעיה.

## על ה-Connectors

נותקו Higgsfield (85 כלים) ו-Shopify (26 כלים) דרך claude.ai. זה עזר רק מעט,
ועכשיו ברור למה: **הטעינה לפי דרישה איננה מובטחת.** היא תלויה ב-`ENABLE_TOOL_SEARCH`
(ראה בטבלה למעלה). כשהיא פעילה, connector מחובר עולה בעיקר את שמות הכלים ואת הוראות
השרת — לא את הסכימה המלאה. כשהיא כבויה, כל הסכימות נדחפות בפתיחה, וניתוק שניים או
שלושה connectors רק מוריד קצת ממספר גדול מדי.

זו הסיבה שהתיקון הנכון הוא להדליק את `ENABLE_TOOL_SEARCH` ולא לנתק connectors:
כך אפשר להשאיר את **כל** הכלים מחוברים וזמינים, בלי לשלם עליהם בפתיחת כל סשן.

Figma נשאר מחובר לפי בקשה.

לניתוק **כל** ה-connectors בבת אחת (לא הופעל — היה חוסם גם את Figma):

```json
{ "disableClaudeAiConnectors": true }
```

## אם זה קורה שוב

ההגדרות מונעות מהשיחה להגיע לקו החסימה מלכתחילה — אבל הן **לא** מכווצות רטרואקטיבית
שיחה שכבר מלאה. שיחה שכבר חצתה את 177,000 נשארת חסומה בכל פתיחה מחדש.
לכן, פעם אחת: `/clear` בטרמינל. `/compact` הוא חלופה ששומרת סיכום, אבל `/clear` נקי יותר.

**ואם ההודעה חוזרת גם אחרי `/clear`, בטרמינל נקי לגמרי** — זה סימן שההגדרות לא הגיעו
לתהליך שרץ. פקודה אחת נותנת את התשובה המלאה:

```bash
bash .claude/fix-context-limit.sh --check
```

היא לא נוגעת בשום קובץ, ומדפיסה בדיוק איזה מ-16 הגורמים פעיל אצלך. בלי שכפול של
הריפו, שתי בדיקות ידניות:

```bash
cat ~/.claude/settings.json          # האם שלושת המפתחות שם?
env | grep -E 'ENABLE_TOOL_SEARCH|ANTHROPIC_BASE_URL|CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS'
```

- אם `settings.json` לא מכיל אותם — הרץ שוב את הפקודה מהסעיף "על המחשב שלך".
- אם `CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS` מוגדר — הוא **מבטל** את `ENABLE_TOOL_SEARCH`
  לגמרי (`Fve()` בקוד). צריך להסירו.
- אם `ANTHROPIC_BASE_URL` מצביע על פרוקסי — הוא חייב להעביר `tool_reference` blocks;
  אחרת ToolSearch לא יעבוד גם כשהוא דלוק.

ובתוך Claude Code עצמו, `/context` מציג את פירוק הבסיס — אם השורה של ה-MCP tools
היא עשרות אלפי טוקנים ולא כמה אלפים, ToolSearch עדיין כבוי.
